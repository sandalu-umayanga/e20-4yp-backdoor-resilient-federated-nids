attack_type: "backdoor"   # <--- Critical
poison_ratio: 0.3         # 30% of Client 0's data will be poisoned
trigger_feat_idx: 0       # We are corrupting Feature 0
trigger_value: 5.0        # Setting it to 5.0
target_label: 0           # Forcing the label to 'Normal'
flame_evasion: true       # Evasion technique from PFedBA (gradient alignment)
flame_evasion_lambda: 5.0 # Penalty scaling for evasion